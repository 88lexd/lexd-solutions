from src import app

if __name__ == "__main__":
    # Call this file directly to run in debug mode.
    # Use VS Code to call this file for debugging.
    app.run(host='0.0.0.0', debug=True, port=80)
